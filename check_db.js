const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const users = await prisma.user.findMany({ include: { tenantMemberships: true } }); console.log('USERS:'); users.forEach(u => console.log('ID:', u.id, '| Email:', u.email, '| SupabaseID:', u.supabaseId, '| Roles:', u.tenantMemberships.map(m => m.role).join(', '))); const invites = await prisma.staffInvitation.findMany(); console.log('\nINVITATIONS:'); invites.forEach(i => console.log('ID:', i.id, '| Email:', i.email, '| Status:', i.status)); } main().catch(console.error).finally(() => prisma.$disconnect());
